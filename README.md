# Gen_Ai_Assignment2
thisa is a simple webpage that take in some user provided information and generates a prompt to be sent to a Provided Google AI studio agent and allow the users limited editiable peramitors for the AI agent

I chaose to use Google Gemini to generated the code for the website becasue my ubderstanding of HTML is bacsic at best and i have no knowlege of JaveScript 

prompts used to generate the HTML/css code for the assigment 
I need a simple HTML coded  website that will take in a prompt from a user then feed it into a AI agent.  This website should have a dropdown menu that will contains a list of roles the AI agent can use to complete the users prompt there should also be a temperature slider that goes from 0.0 to 2.0 that the user can set this number. Which should be sent to the AI Aswell in the form of a temperature parameter. The AI should return a response in a separate textbox on the website and the final prompt provider to the AI this will including the users prompt, the system prompt, and the role selected by the user in the drop down box this final prompt should be displayed on the website in a separate textbox from the response.

this is not only the prompts used to create the assignment here is a link to whole google gemini chat thread if you want to look over it 
https://gemini.google.com/share/d2d0f804f2e2


useing the site
when you open the site it has a text input for an API key from Google AI studio 
then you have a drop down for selecting a role for the AI this will change how the response is written by changering the tone and character of the AI
next is the Temperature slider which goes from 0.0 to 2.0 this will change how creative the AI is when writting the response lower give more podicible reponses and higher give more of a creavite human sounding response 
next to the Temperature slider is the thinking level selector this changes how much time and fact checking the AI will perform 
the last imput is the User request this is where a user would write out ther prompt for the AI to run 

the last 2 sections are ouputs the first is the final prompt this show the wohle prompt sent to the AI based on the users input in the above boxes it has a system instuction sections an agent role and finally the user prompt 
the last box will show the AI responce to the finmal promopt 
