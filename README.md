# Gen_Ai_Assignment2

prompts used to generate the HTML/css code for the assigment 

I need a simple HTML coded  website that will take in a prompt from a user then feed it into a AI agent.  This website should have a dropdown menu that will contains a list of roles the AI agent can use to complete the users prompt there should also be a temperature slider that goes from 0.0 to 2.0 that the user can set this number. Which should be sent to the AI Aswell in the form of a temperature parameter. The AI should return a response in a separate textbox on the website and the final prompt provider to the AI this will including the users prompt, the system prompt, and the role selected by the user in the drop down box this final prompt should be displayed on the website in a separate textbox from the response.

can you set the website up to work with Google Ai studio

these are not all the prompts used to create the assignment here is a link to whole google gemini chat thread if you want to look over it 
